version: "0.0.2"
license: "Apache-2.0"
description: ESP Emote Gen Player
url: "https://github.com/espressif2022/esp_emote_gen_player"
repository: "https://github.com/espressif2022/esp_emote_gen_player.git"

dependencies:
  idf:
    version: '>=5.0'
  espressif2022/esp_emote_gfx:
    version: '^3.0.0'
    public: true
  espressif/esp_mmap_assets:
    version: '*'
    public: true
  # NanoSoul: use the IDF built-in `json` (cJSON) instead of espressif/cjson so
  # the whole project has a single cJSON provider (storage_sd also uses `json`).
  # Avoids a duplicate-symbol clash between the two providers.