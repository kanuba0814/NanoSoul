# NanoSoul 载板 —— 嘉立创/JLCPCB 带贴片(PCBA)下单文件包

生成自 `pcb/output/NanoSoul/NanoSoul.kicad_pcb`（**DRC 0 violation / 0 unconnected / 0 schematic-parity**，62 项检查全 error 零 ignore；无铺地，GND 走线布通）。
**下单料号已对嘉立创 SMT 核对、全部现货可自动采购**（旧单两处"邮寄"已修：`U6` IMU 改同族现货 **QMI8658A**(C 版 C2842151 JLC 停产)、`R9` 电流 shunt 改 **1206 50mΩ**(0805 50mΩ JLC 无现货)）。

## 上传什么、传到哪
| 文件 | 用途 | 传到 |
|---|---|---|
| **`NanoSoul_Gerbers.zip`** | 板子本身（铜/阻焊/丝印/钢网/外形/钻孔） | 下单页「**Gerber**」 |
| **`NanoSoul_BOM.csv`** | 物料表（料名/位号/封装/LCSC） | PCBA「**BOM**」 |
| **`NanoSoul_CPL.csv`** | 贴片坐标（位号/X/Y/层/旋转） | PCBA「**CPL / 坐标文件**」 |
| `gerbers/` | 松散 Gerber（核对用，下单用上面的 zip） | — |

## 板子制造参数（下单页填这些）
- 层数 **2 层**；尺寸 **≈96.8 × 92.2 mm**；板厚 **1.6 mm**；铜厚 1oz；表面处理 HASL 无铅(或沉金)。
- 外形：圆角矩形（Ø116 圆四边切平），**最外围 1mm 白边**（铜距外缘 ≥1mm，常规铣外形余量）。
- **⚠️ 中部 15.5 × 59 mm 矩形通槽（铣槽 routed slot）** —— 在 Edge.Cuts 上，务必让厂家**按内框铣穿**（竖插开发板的底面件/FPC/microSD 从这穿过）。系统没自动识别内槽就备注说明。
- 孔：**4 个 NPTH 安装孔（M3=Ø3.2）**，给后续支架。M2 定位孔已取消。
- **无铺地**：GND 当普通网已布通（本板 0/0）。要双层铺地可出板前 KiCad GUI 加，不影响下单。
- 最小线宽/间距：本设计 ≥0.2mm（仅 USB-C(J2) 连接器内部 0.09mm 例外，已在 .kicad_dru；元件自身几何、可造），常规工艺即可。

## ⚠️ 贴片前必须确认（不看会装错/缺料）
1. **开发板不在本 BOM 里**。载板只贴自己的件；开发板靠 J3/J4 母座对插。
2. **BOM 30 行料号已全带 LCSC、全为 JLC 现货可自动采购**（无留空/邮寄/停产）。`R9`=50mΩ 电流采样 shunt 用 1206 现货 `C375525`(Yageo RL1206FR-070R05L,250mW)；`U6`=IMU 用 `C3021082`(QMI8658A,C 版 C2842151 停产)。阻值标定在固件，shunt 0.02~0.1Ω 都行。
3. **THT 连接器自己手焊（推荐）**：`J3`/`J4`（1×20 立式母座，开发板靠它对插、**行距 17.80mm**）、`J5`/`J6`/`J7`（1×6 2.54 排针，电机束线）、`J1`（JST-PH 电池）。
   - 这几件 **SMT 不贴**。母座 J3/J4 尤其建议自备 + 手焊，保证行距精度与可插拔。要省事也可在 JLC 选「**THT 装配**」加项（J1 已带 `C131337`；1×6 排针参考 `C37208`；1×20 单排母座按需在 LCSC 选）。
   - `J2`(USB-C)、`J8`(JST-SH 光照) 都是 **SMD，SMT 能贴**。
4. **极性件旋转必核对**：BOM「类型/备注」标了「极性/旋转核对」的件（所有 IC、二极管 D1-D5、电解 C8、**USB-C J2**）。**上传 CPL 后看 JLC 预览，逐个核对 pin1/阴极/正极/USB-C 朝向** —— KiCad 与 JLC 库朝向常差 90/180°，这是 PCBA 最易错的点。
5. **库存薄的件下单前查**：`L1`(`C2047296` 2.2µH)/`L2`(`C703091` 4.7µH) 6.3×6.3 功率电感库存偏薄，缺货就换同封装高 Irms 同感量件。

## 关键料号（已填进 BOM，30/30 行带 LCSC、全现货）
QMI8658A `C3021082`(U6,SPI；C 版 C2842151 停产→同族 A 版,脚位/驱动同) · R9 shunt `C375525`(1206 50mΩ 1% 250mW；0805 50mΩ JLC 无现货) · IP5306-I²C `C488349`(必 I²C 版) · TB6612FNG `C141517`(U4/U5) · MT3608 `C84817`(U3) · DW01A `C351410`(U1) · FS8205A `C908265`(Q1) · SS34 `C908680`(D4/D5) · USB-C `C165948`(J2) · 电感 `C2047296`/`C703091`(L1/L2) · C8 电解 `C4747956` · F1 PTC `C207025`(0805L150) · D1-3 红LED `C84256` · SW1 `C720477`(TS-1088) · J1 `C131337` · J8 `C495539`(TBT 吸嘴带版) · CC 下拉 5.1k `C23186`(R5/R6) · 其余 R/C 按值取 JLC 基础库料号。
保护链 **DW01A+FS8205A** 与电机 **PTC(F1)+shunt(R9)** 已在板（硬需求）。

## 复核清单（出板前）
- [x] BOM 30 行全带 LCSC 现货（`R9`=1206 `C375525`、`U6`=`C3021082`，旧单两处"邮寄"已修）。
- [ ] 板框 ≈96.8×92.2、中部 15.5×59 通槽按内框铣穿。
- [ ] CPL 预览里极性件（含 USB-C）旋转都对。
- [ ] THT 件（J1/J3/J4/J5/J6/J7）确定走 THT 装配还是手焊；J3/J4 行距 17.80mm。
- [ ] `L1/L2` 实时库存够。

> 重新生成本包：`pcb/circuit/fab_bom_cpl.py`（BOM/CPL）+ kicad-cli 导 Gerber/钻孔/坐标。改板先改 `pcb/circuit/*.py` 重跑流水线（见 skills/nanosoul-pcb）。
