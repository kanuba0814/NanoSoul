name: build

on:
  pull_request:
  push:
    branches:
      - develop
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Toolchain pin required
        run: |
          echo "ESP-IDF version is not pinned yet."
          echo "Update this workflow after the team lead fixes the toolchain version."
          exit 1

