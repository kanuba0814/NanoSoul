name: host-test

on:
  pull_request:
  push:
    branches:
      - develop

jobs:
  host-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Host test pin required
        run: |
          echo "Host test workflow is a placeholder until ESP-IDF and host test harness are pinned."
          exit 1

